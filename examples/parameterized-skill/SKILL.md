# Parameterized Skill

## Description

A skill that demonstrates the use of typed parameters including string, enum, number, and boolean types.

## Instructions

Analyze code based on the configured parameters:
- Use the `language` parameter to determine the target language
- Use the `strictness` parameter to control analysis depth
- Limit output to `max-issues` results
- Show extra detail when `verbose` is enabled
